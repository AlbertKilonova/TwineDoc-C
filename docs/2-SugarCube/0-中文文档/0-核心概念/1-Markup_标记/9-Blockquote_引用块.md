---
title: Blockquote_引用块
description: 文本缩进与引用效果
---

<div v-pre>

# Blockquote_引用块

如果你想引用一段名言，或者仅仅是想给文本增加一些缩进效果，可以使用右尖括号 `>`。

### 使用方法

每增加一个 `>` 表示嵌套层级加深一级。

```SugarCube
> 第一行引用
> 第二行引用
>> 这里是嵌套的第二层引用（哇，感觉很有层次感呢）
```

渲染后的效果等同于 HTML 的 `<blockquote>` 标签，会带上优雅的缩进。

</div>